# V5 Hybrid Gauntlet AI Benchmark

This spec starts the next AI lane after both prerequisite gates pass:

- V3 race-aware AI beats frozen production Grove V2 at or above 90/100 on 50 rich-score maps.
- V4-TR tower/worker/mercenary AI beats V3 random race at or above 90/100 on 50 rich-score maps.

## Product Goal

Build `v5`, a general melee AI that can beat a two-opponent allied team made from the current strong lanes:

- opponent A: current V3 race-aware AI;
- opponent B: current V4-TR tower/worker/mercenary AI.

Required benchmark gate:

- 50 sampled rich-score maps.
- 100 total matches.
- V5 plays alone on one team.
- V3 and V4-TR play allied on the opposing team.
- Each benchmark match is a simultaneous 1v2 game. It is not a sequential gauntlet or wheel-war format.
- V5 race is seed-randomized from `grove | ember`.
- V3 race is seed-randomized from `grove | ember`.
- V4-TR remains fixed to its constrained tower/merc implementation.
- Spawn/team assignment and which opposing slot receives V3 versus V4-TR are seed-randomized but reported.
- Required pgl result: V5 wins at least 90/100.

## Constraints

V5 may use the full normal melee toolkit unless a later spec narrows it:

- workers, ordinary economy, expansions, and repairs;
- race production and tech;
- healing buildings and support units;
- neutral mercenaries and items;
- group tactics, harassment, and coordinated closeout.

V5 must not:

- weaken V3 or V4-TR to pass;
- use benchmark-only hidden resources, vision, unit stats, or special commands;
- fork simulation, command-frame, hosted play, static play, SDK execution, or benchmark execution;
- tune only a combat-only mode.

## Benchmark CLI

Add a dedicated CLI:

```bash
npm run benchmark:ai-v5-vs-hybrid -- --seed v5-hybrid-50-2026-06-09 --map-count 50 --workers 95
npm run benchmark:ai-v5-vs-hybrid -- --seed v5-hybrid-50-2026-06-09 --map-count 50 --workers 95 --dashboard
npm run benchmark:ai-v5-vs-hybrid -- --seed v5-hybrid-50-2026-06-09 --map-count 50 --dry-run
npm run benchmark:ai-v5-vs-hybrid -- --seed v5-hybrid-50-2026-06-09 --map-count 50 --match "cobaltVale v5 north" --details
```

Dry-run output must include:

- seed;
- selected map ids;
- match count;
- every match name;
- V5 side, race, AI version, and policy version;
- V3 side, race, AI version, and policy version;
- V4-TR side, race, AI version, and policy version;
- enough manifest detail to prove the two-opponent team is exactly `v3 + v4-tr`.
- enough manifest detail to prove V3 and V4-TR are in the same match on the same opposing team.

Execution output must include:

- total V5 wins, raw match count, and win rate;
- breakdown by V5 race;
- breakdown by V3 race;
- breakdown by opponent slot assignment;
- side-balanced per-map results;
- elapsed time, CPU time, worker count, seed, selected maps, and code revision evidence.

Dashboard execution output must write the full run through the shared `run-contract-v2` dashboard store as an `ai-specialized-benchmark` with `targetPlayerId: "v5"` and print only a compact run summary.

## Acceptance Evidence

Implementation is not complete until current evidence proves all of the following:

- Unit tests prove the planner/version contract accepts `v5`.
- Dry-run tests prove the hybrid benchmark creates 50-map / 100-match setup and reports V5, V3, and V4-TR versions explicitly.
- Benchmark tests prove V3 and V4-TR are allied opponents rather than separate evaluations.
- V5 policy tests prove it remains a normal melee AI, not a hidden anti-V3 or anti-V4-TR special case.
- Local `npm run build` passes.
- pgl benchmark evidence from the dedicated CLI shows at least 90/100 V5 wins against the hybrid `v3 + v4-tr` allied team.
- The pgl result reports seed, selected maps, worker count, wall time, CPU time, and exact code revision.

## Starting State

- V3 prerequisite evidence on 2026-06-09: five 50-map / 100-match pgl dashboard runs scored `90/100`, `93/100`, `91/100`, `91/100`, and `92/100` against frozen production Grove V2.
- V4-TR prerequisite evidence on 2026-06-09: four 50-map / 100-match pgl dashboard runs scored `96/100`, `96/100`, `97/100`, and `96/100` against V3 random race.
- The first V5 task is benchmark scaffolding and a baseline measurement. Do not tune V5 before the benchmark can prove the exact 1v2 setup.

## Current Evidence

- `2026-06-09T06-26-41-000Z-10nj074`, seed `v5-hybrid-50-2026-06-09a`, dashboard path `/home/ubuntu/sketch-rts-benchmark-main/.benchmark-dashboard`, current code revision `7daae6f` plus this worktree's uncommitted V5 scaffold.
- Result: `50/100` V5 wins over simultaneous allied `v3 + v4-tr`, below the required `90/100` gate.
- Race split: V5 Grove `21/50`, V5 Ember `29/50`; against V3 Grove `29/50`, against V3 Ember `21/50`.
- Runtime: `25,602.444 ms` wall, `716,192.229 ms` CPU.
- Zero-win side-balanced maps in this run: `ochreRidge`, `frostMeadow`, `pineTangle`, `lanternFord`, `cinderHeath`, `cobaltVale`, `hazelCircuit`, `mapleCircuit`, `verdigrisSpire`, `auricDelta`, `fernBarrow`, `plumTarn`.
- Focused verification after adding the V5 benchmark scaffold, opponent slot randomization, and summary reporting: `npx vitest run scripts/ai-v3-vs-prod-v2-benchmark.test.ts scripts/ai-v4-tr-vs-v3-benchmark.test.ts scripts/ai-v5-vs-hybrid-benchmark.test.ts src/ai/policy.test.ts src/ai/planner-context.test.ts src/ai/policy/production-model.test.ts src/ai/policy/tower-merc-policy.test.ts src/ai/policy/skirmish-tactics.test.ts src/ai/benchmark/control.test.ts scripts/ai-playtest.test.ts && npm run build` passed with current focused coverage.

Interpretation:

- The benchmark now proves the requested same-match 1v2 setup.
- The current `v5` policy has early V5-specific economy/expansion/contested-natural slices, but it is still far below the hybrid 1v2 target.
- Next work must diagnose repeated V5 losses from exact match detail and change V5 behavior with tests before rerunning 50-map pgl benchmarks.

### 2026-06-09 Current Baseline

- Prerequisite gates before this baseline:
  - V3 vs frozen production V2 latest audit: `v3-frozen-current-audit-50-2026-06-09f`, run `2026-06-09T22-19-00-161Z-1uiid6l`, workers `95`, V3 total `95/100`.
  - V4-TR vs current V3 latest audits: `v4-tr-current-audit-50-2026-06-09b/c/d`, runs `2026-06-09T22-17-44-852Z-13ncqxg`, `2026-06-09T22-18-18-905Z-13xccmf`, and `2026-06-09T22-18-31-996Z-133djji`, workers `95`, V4-TR totals `91/100`, `94/100`, and `92/100`.
- V5 current hybrid baseline: `v5-hybrid-current-audit-50-2026-06-09a`, run `2026-06-09T22-22-16-423Z-1w3a4ra`, workers `95`, wall time `21.80s`, CPU time `878988.859ms`, V5 total `46/100`.
- Race split from that run:
  - V5 Grove vs V3 Ember: `7/24`.
  - V5 Grove vs V3 Grove: `10/26`.
  - V5 Ember vs V3 Grove: `20/24`.
  - V5 Ember vs V3 Ember: `9/26`.
- Zero-win maps in that run: `yarrowFen`, `cobaltVale`, `ironMoss`, `ochreRidge`, `reedBasin`, `russetBrook`, `obsidianBrook`, `bluebellHeath`, `tealFissure`, `lanternFord`, `plumTarn`, `briarToll`.
- Exact `yarrowFen v5 south` showed a fast Grove V5 loss: V5 expanded around 256s but had no durable tower/healing position when V3 Ember and V4-TR mercenary pressure converged, then died around 365s. A near-tower-gold reserve hypothesis was tested with a RED policy test and a temporary base-defense reserve helper, but the exact replay still lost at 357s; that hypothesis was reverted because it added complexity without flipping the real failure.
- Fresh verification after reverting that failed V5 hypothesis: `npx vitest run src/ai/policy.test.ts src/ai/policy/skirmish-tactics.test.ts src/ai/policy/tower-merc-policy.test.ts scripts/ai-v3-vs-prod-v2-benchmark.test.ts scripts/ai-v4-tr-vs-v3-benchmark.test.ts scripts/ai-v5-vs-hybrid-benchmark.test.ts src/ai/benchmark/control.test.ts && npm run build` passed with `393` tests.
- Follow-up TDD coverage now verifies V4-TR exact benchmark replay flags are present in the playtest command manifest and V5 execution summaries include `byOpponentOrder` for the simultaneous `v3,v4-tr` versus `v4-tr,v3` opponent-slot split.
- Commit-level pgl dashboard baseline on committed revision `0f00ef1`: `v5-commit-0f00ef1-50-2026-06-09a`, run `2026-06-09T22-51-21-777Z-1ao6p2a`, workers `95`, wall time `21.92s`, CPU time `873605.222ms`, V5 total `52/100`.
- Commit-level breakdown:
  - V5 Grove: `27/50`; V5 Ember: `25/50`.
  - Against V3 Grove: `25/50`; against V3 Ember: `27/50`.
  - Opponent order `v3,v4-tr`: `32/51`; opponent order `v4-tr,v3`: `20/49`.
  - Zero-win maps: `copperWeald`, `frostMeadow`, `duskGrove`, `ochreRidge`, `chalkFen`, `amberReach`, `ashVale`, `tealFissure`, `russetBrook`, `bluebellHeath`, `reedBasin`.

### 2026-06-10 Veteran-Core Work

- Baseline after V3/V4 dashboard split reporting: seed `v5-current-veteran-book-50-2026-06-10a`, run `2026-06-10T19-43-05-012Z-142fpel`, workers `95`, V5 total `57/100`.
- Decoupling V5 worker pressure from the main 1v2 focus target raised the same seed to `58/100`, run `2026-06-10T19-50-41-656Z-142fpel`.
- Allowing safe stopped retreaters to rejoin a stolen-first-natural punish raised the same seed to `60/100`, run `2026-06-10T20-11-46-431Z-142fpel`.
- Failure aggregation from that `60/100` run showed a strong veteran signal: V5 wins averaged `7.42` weighted star value and `3.93` starred bodies, while V5 losses averaged only `0.55` weighted star value and `0.30` starred bodies; losing opponents averaged `5.88` weighted star value.
- V5-only near-star preservation was added: a unit within 16 XP of a star and at less than half health uses the same recovery path as established veterans. Same seed result: `62/100`, run `2026-06-10T20-19-06-867Z-142fpel`; after reverting a negative detachment experiment, the retained state reproduced `62/100` in run `2026-06-10T20-26-57-229Z-142fpel`.
- Negative experiment: excluding near-star/veteran core units from V5 worker-pressure detachments dropped the same seed to `55/100`, run `2026-06-10T20-24-34-872Z-142fpel`; this was reverted.
- Negative experiment: pausing V5 neutral objectives during first-expansion conversion dropped the same seed to `54/100`, run `2026-06-10T20-33-43-708Z-142fpel`; this was reverted. Exact `amberReach v5 north` looked like a bad mercenary-objective split, but the 50-map result showed objective tempo and item/XP access were still worth more than the extra home hold.
- V5-only experience-book objective priority was added after a RED test showed V5 preferred a nearer `stormStaff` camp over a slightly farther `experienceBook` camp. Same seed stayed flat at `62/100`, run `2026-06-10T20-38-41-676Z-142fpel`; keep this under cross-seed watch because it is strategically coherent but not yet a measured score gain.
- Fresh current-seed baseline for the next veteran-core pass: seed `v5-veteran-core-50-2026-06-10b`, run `2026-06-10T20-49-04-578Z-14ygrcy`, workers `95`, V5 total `59/100`. Split was V5 Grove `32/52`, V5 Ember `27/48`, against Grove V3 `29/50`, against Ember V3 `30/50`. Failure aggregation again showed final veteran survivorship as a strong signal: V5 wins averaged `6.93` star-value and `3.73` starred bodies, while losses averaged `0.02` star-value and `0.02` starred bodies; losing opponents averaged `6.34` star-value.
- Negative experiment: exact `quietMire v5 south` showed a `315s` V5 `workerPressure` command sending a 124xp veteran ravager-led group at a V4-TR worker while V3 and V4-TR armies were converging. A narrow V5 joint-army worker-pressure yield made that exact plan return no entries, but the same 50-map seed dropped from `59/100` to `56/100`, run `2026-06-10T21-01-01-866Z-14ygrcy`; this was reverted. Do not broadly suppress V5 worker pressure around veteran units without replacing the lost tempo.
- Negative experiment: the same exact `quietMire v5 south` showed a safe 90xp cinderRunner held out of the raid by a retreat claim, so a narrow worker-pressure retreater rejoin was tested. It made the exact `315s` plan include that unit, but the same 50-map seed dropped further to `55/100`, run `2026-06-10T21-07-19-502Z-14ygrcy`; this was reverted. Rejoining retreaters into workerPressure is not the right veteran-core lever by itself.
- V5 Ember wounded-core support slice: when a V5 Ember core already has multiple wounded fighters and no healer, `cinderSpire` trains `emberAcolyte` before the first `sparkArcher`; ordinary Ember first-spire behavior is unchanged. Same new baseline seed stayed at `59/100`, run `2026-06-10T21-14-25-152Z-14ygrcy`. The older veteran-book seed improved from the retained `62/100` to `63/100`, run `2026-06-10T21-15-14-142Z-142fpel`; split was V5 Grove `27/50`, V5 Ember `36/50`, so the next gap is Grove V5.
- Neutral experiment: raising V5 Grove's late `knight` target from 2 to 4 passed the local training-choice test but did not change either audited seed: `v5-current-veteran-book-50-2026-06-10a` stayed `63/100` in run `2026-06-10T21-18-24-179Z-142fpel`, and `v5-veteran-core-50-2026-06-10b` stayed `59/100` in run `2026-06-10T21-18-59-343Z-14ygrcy`; this was reverted as no measurable gain.
- V5 direct residual-building closeout slice: in dead-economy 1v2 closeouts with no enemy army, V5 now directly attacks a nearby residual building instead of only attack-moving to its point. This targets Grove timeout cases like `wispQuarry v5 north`, where V5 had a veteran core and enemy supply was gone but residual buildings survived to 2400s. The older veteran-book seed improved from `63/100` to `64/100`, run `2026-06-10T21-27-26-053Z-142fpel`; split was V5 Grove `28/50`, V5 Ember `36/50`. The newer baseline seed stayed `59/100`, run `2026-06-10T21-28-02-113Z-14ygrcy`.
- Neutral/reverted experiment: exact `runeMeadow v5 north` showed V5 local defenders stuck under retreat claims while a high-XP V4-TR mercenary hit the main. A narrow V5 small-base pickoff test and implementation made the exact planner attack that target, but the exact match got worse: loss moved from `481.05s` to `452.2s`, and V5 enemy kills dropped from `11` to `2`. The 50-map veteran-book seed stayed flat at `64/100` in run `2026-06-10T21-45-53-916Z-142fpel`, with Grove `+1` and Ember `-1`, so this was reverted. Do not add local XP-feed targeting without preserving the existing main-defense target priority.
- Negative experiment: exact `mossglassRun v5 south` showed V5 losing two level-2 units during worker-pressure and forward fights, then dying with no stars while V3 reached `16` star-value. A narrow V5 worker-pressure detachment chooser preserved veterans when three nearby low-XP units could still raid; it flipped that exact to a V5 win at `768.05s` with V5 `10` star-value. The 50-map veteran-book seed still dropped from `64/100` to `58/100` in run `2026-06-10T22-06-10-780Z-142fpel`, with Grove falling to `25/50` and Ember to `33/50`, so this was reverted. Do not optimize worker-pressure unit selection around XP alone; the raid tempo and local force geometry are carrying more value than the protected XP in aggregate.
- Negative experiment: V5 Grove exact snapshots showed mature Grove often has sanctum/workshop online but few or no priests/heavy units. A narrow V5 Grove "first priest for wounded veteran core" reserve bypass passed its RED test, but the 50-map veteran-book seed dropped from `64/100` to `59/100` in run `2026-06-10T22-19-33-690Z-142fpel`; Grove fell to `23/50` while Ember stayed `36/50`. This was reverted. Do not force first-priest spending solely from veteran presence; it disrupts broader Grove timing more than it preserves XP.
- Negative experiment: a runner-level V5 workerPressure integrity gate dropped a `workerPressure` attack command if earlier tactical scripts had already reserved any unit from that planned detachment. The RED test reproduced the opalFen-style symptom where `focusFire` left only one late footman in a workerPressure attack. The 50-map veteran-book seed dropped from retained `64/100` to `56/100` in run `2026-06-10T22-36-56-713Z-142fpel`; Grove fell to `26/50` and Ember to `30/50`. This was reverted. Do not make workerPressure all-or-nothing after command conflict filtering; partial raids still carry enough aggregate tempo that hard integrity loses more than it saves.
- V5 cleared-natural attack-wave threshold: after the first natural no longer needs clearing, V5 1v2 ordinary `attackWave` waits for a seven-unit wave even before the second base is actively mining. This keeps the first reusable core together without suppressing workerPressure, objectiveControl, or closeout paths. The veteran-book seed stayed flat at `64/100` in run `2026-06-10T22-45-21-394Z-142fpel` with Grove rising `28 -> 30` and Ember falling `36 -> 34`; the newer veteran-core seed improved from `59/100` to `61/100` in run `2026-06-10T22-46-08-166Z-14ygrcy`, with Grove `32 -> 33` and Ember `27 -> 28`. Keep for now under continued seed watch.
- V5 unmined-first-expansion direct-chase guard: exact `wispQuarry v5 north` showed `v5MainApproachDetachmentPickoff` and other direct-target attack-wave branches turning a melee target outside the first expansion defense into a deep chase before the second mine was active. The guard keeps direct focus for close/base targets and ranged attackers actively pressuring allied assets, but excludes off-line melee chase targets until V5 has two active mining bases. Same seed `v5-merc-bank-50-2026-06-10a` improved from retained `64/100` to `65/100`, run `2026-06-11T02-18-18-150Z-ovkyak`; flips were `+2/-1` with Grove `29/49 -> 30/49` and Ember `35/51 -> 35/51`.
- V5 fresh-natural emergency tower targeting: exact `yarrowFen v5 north` showed V5 banking for a tower, then placing it at the first natural while V3's first hit was already attacking a main-lane target near core production. The fix keeps natural towers when the natural is truly under close pressure, but lets emergency defense choose the main when enemy attack targets prove the main-production lane is being cracked. Same seed `v5-merc-bank-50-2026-06-10a` improved from `65/100` to `66/100`, run `2026-06-11T02-52-26-973Z-ovkyak`; flips were `+2/-1` with gains on `graniteBloom v5 north` and `mossglassRun v5 north`, loss on `pearlCircuit v5 south`, and V5 Ember rising `35/51 -> 36/51`.
- V5 first-expansion local mercenary objective: exact `umberCauseway v5 south` showed V5 holding first-expansion gold while a local guarded contract-archer camp stayed available, then losing early to V4-TR mercenary pressure. `objectiveControl` now lets V5 clear a local guarded mercenary camp during the first-natural conversion pause while keeping ordinary v2 two-opponent merc detour guards intact. Exact `umberCauseway v5 south` flipped from V4-TR win at `457.6s` to V5 win at `834.05s` with V5 `mercenaryKills=34`. Same seed `v5-merc-bank-50-2026-06-10a` improved from `66/100` to `68/100`, run `2026-06-11T04-26-18-423Z-ovkyak`; flips were `+3/-1` with gains on `yarrowFen v5 south`, `umberCauseway v5 south`, and `mallowRun v5 south`, loss on `auricDelta v5 north`.
- Fresh verification for the local mercenary objective slice: `npx vitest run src/ai/policy.test.ts` passed with `353/353`, and `npm run build` completed successfully.
- Fresh focused verification after the retained veteran-core slices: `npx vitest run src/ai/policy.test.ts src/ai/planner-context.test.ts src/ai/policy/production-model.test.ts src/ai/policy/tower-merc-policy.test.ts src/ai/policy/skirmish-tactics.test.ts src/ai/policy/item-tactics.test.ts src/ai/benchmark/control.test.ts scripts/ai-v3-vs-prod-v2-benchmark.test.ts scripts/ai-v4-tr-vs-v3-benchmark.test.ts scripts/ai-v5-vs-hybrid-benchmark.test.ts scripts/ai-playtest.test.ts && npm run build` passed with `460` tests.
- V5 first-heal-before-second-tower slice on 2026-06-11: exact `juniperDelta v5 north` showed two-base Grove V5 had multiple wounded defenders and one completed expansion tower, but spent scarce gold on a second tower before any moon well. V5 then entered the first 1v2 pressure fight with no healing and lost almost all combat units by `345s`. The healing-well policy now lets the first healing building beat a second tower only when V5 is in a two-opponent game, has at least two completed town halls, already has a completed tower, has no healing building, and has a wounded two-base defender group. Exact `juniperDelta v5 north` flipped from V4-TR win at `513.6s` to V5 win at `761.65s`; exact `saffronFen v5 south` also flipped. Same seed `v5-merc-bank-50-2026-06-10a` improved from `68/100` to `70/100`, run `2026-06-11T04-56-22-941Z-ovkyak`, with flips `+2/-0`.
- Negative experiment: exact `lanternFord v5 north` showed a wounded two-base V5 core split by a far north creep claim after local mercenary control. A narrow V5 recall for far creep claims with an unhealed wounded home defender delayed the exact loss from `506.6s` to `696.2s`, but the same 50-map seed dropped from `70/100` to `69/100` in run `2026-06-11T05-19-25-056Z-ovkyak`; this was reverted. Do not solve this lane by broadly recalling objective claims after the first two-base wound. The next useful hypothesis is the later release/rejoin and army conversion problem: around `420s` V5 has full-health defenders near the moon well, but retreat claims and local defense rules still fail to form a decisive counterfight before V3's three-base army scales.
- Fresh current-head prerequisite checks on 2026-06-11 still pass after the V5 work: V3 vs frozen V2 scored `95/100` on seed `v3-current-head-gate-50-2026-06-11a`, run `2026-06-11T04-42-03-273Z-1ewoygv`; V4-TR vs current V3 scored `94/100` on seed `v4-tr-current-head-gate-50-2026-06-11a`, run `2026-06-11T04-42-30-789Z-ao6of1`.
- Fresh verification for the first-heal-before-second-tower slice: `npx vitest run src/ai/policy.test.ts src/ai/policy/skirmish-tactics.test.ts src/ai/policy/tower-merc-policy.test.ts src/ai/benchmark/control.test.ts scripts/ai-v5-vs-hybrid-benchmark.test.ts` passed with `418/418`, and `npm run build` completed successfully.
- V5 tower/merc bank baseline before the micro pass: seed `v5-merc-bank-50-2026-06-10a`, run `2026-06-11T14-10-40-547Z-ovkyak`, V5 total `72/100`, with V5 Grove `31/49` and V5 Ember `41/51`.
- V5 approach-discipline micro slice on 2026-06-11: exact `heatherCircuit v5 south` showed V5 Grove had three bases, high peak supply, and a large army, but direct-targeted a distant healthy melee front walking toward the home base, leaving formation and dying. V5 now filters healthy distant melee approach targets for the 1v2 direct-target branches while still allowing close, wounded, ranged pressure, and building-defense targets. Exact `heatherCircuit v5 south` flipped from V4-TR win at `1189.85s` to V5 win at `1343.2s`; same seed improved from `72/100` to `73/100`, run `2026-06-11T16-35-49-108Z-ovkyak`, with V5 Grove `32/49` and V5 Ember `41/51`.
- Negative micro experiment: exact `mirrorHeath v5 north` showed `workerPressure` recalling a local pressure squad when the other opponent's cover army intercepted it. Allowing smaller V5 local worker-pressure squads to fight the cover army flipped that exact from a V3 win at `944.35s` to a V5 win at `1417.1s`, but the same 50-map seed dropped from `73/100` to `70/100`, run `2026-06-11T16-05-37-366Z-ovkyak`; this was reverted. Do not broadly convert covered worker-pressure retreats into fights without a stronger global selector.
- Negative micro experiment: exact `briarToll v5 south` suggested retreat claims were released before a small Ember squad finished resetting, but adding a V5 attack-wave retreat cooldown did not change that exact match at all (`488.6s` V4-TR win both before and after); this was reverted as unproven complexity.
- Micro behavior消融 on the same seed showed the major tactical modules are net positive: disabling V5 `workerHarassment` dropped the score to `59/100`; disabling it only for Grove dropped to `66/100`, only for Ember also dropped to `66/100`; disabling `skirmishPreservation` dropped to `24/100`. Current evidence says the next useful work is not removing harassment or preservation, but improving their boundary decisions.
- Negative late-tech experiment on 2026-06-11: exact `runeMeadow v5 south` showed V5 Grove long games can have `stables`/`sanctum` online while still ending with mostly basic units, so a V5-only mature-tech training bypass was tested. The broad version let two/three-base V5 Grove spend through optional `workshop` and duplicate-production banks on `sanctum`/`stables` units at six combat bodies; it produced priests/summoners/witches but made exact `runeMeadow v5 south` worse, turning the prior timeout into a V4-TR win at `1727.1s`. The same 50-map seed stayed flat at `73/100` in run `2026-06-11T17-36-19-721Z-ovkyak`. A narrower "first mature support only before ten combat bodies" variant restored the exact `runeMeadow` timeout but dropped the 50-map seed to `72/100` in run `2026-06-11T17-44-20-044Z-ovkyak`, with Grove falling `32/49 -> 31/49`. This was reverted. Do not solve V5 by simply spending earlier into Grove casters; the missing piece is a front-line/closeout conversion rule, not earlier support-unit banking by itself.
- Negative closeout experiment on 2026-06-11: exact `runeMeadow v5 south` also showed the late planner empty after V5 was down to two soldiers and three workers, so a V5 worker-assisted dead-economy closeout was tested using the existing dead-economy building scoring. The exact did not flip; it timed out with V5 workers dead and V4-TR rebuilt to `10` workers, making the state worse than the retained baseline. This was reverted. Do not pull ordinary V5 workers into attack-wave closeout directly; the viable closeout fix likely needs residual-worker suppression or production/army conversion earlier, before V5's last workers become the cleanup force.
- Neutral closeout experiment on 2026-06-11: a smaller V5-only two-to-four-soldier dead-economy closeout rule was also tested. It passed the isolated unit policy test but did not flip exact `runeMeadow v5 south`, and the same 50-map seed stayed flat at `73/100` in run `2026-06-11T18-15-05-729Z-ovkyak`. This was reverted as no-gain complexity.
- V5 expansion-tower neutral spacing slice on 2026-06-11: exact `sundialReach v5 south` showed V5's third-base guard tower being placed at `(1956,3176)`, directly beside a live red free camp, which pulled neutral attacks onto the builder and miners; V5 neutral deaths jumped from `2` to `7` between `540s` and `600s`. A broad neutral-aware tower placement flipped the exact but dropped the 50-map seed to `70/100`, so it was narrowed to only unthreatened expansion guard towers. The narrowed version kept main/threatened tower placement unchanged, flipped exact `sundialReach v5 south` from V3 win at `894.3s` to V5 win at `1150.9s`, reduced V5 neutral deaths `8 -> 2`, and improved the standard seed from `73/100` to `75/100` in run `2026-06-11T18-32-34-159Z-ovkyak`. The only aggregate flips were `sundialReach v5 south` and `mirrorHeath v5 north`, both Grove losses with high neutral deaths.
- Negative worker-pressure route experiment on 2026-06-11: exact `ochreRidge v5 south` showed V5 losing a pressure squad while chasing V4-TR workers through a V3 route army. Extending `workerPressureRouteCoverBlockers` so target-at-base workers also considered route blockers reduced neutral deaths but made the exact worse, moving the V3 win from `1204.5s` to `787.1s`, because skipping that pressure let the second opponent's economy stay intact. This was reverted. Do not broadly suppress target-base workerPressure on route cover; the fix needs a more selective conversion, not a retreat.
- Negative third-expansion pressure experiment on 2026-06-11: exact `silverRidge v5 south` showed V5 Grove pulling a five-unit field group into a third expansion claim while enemy armies were forming near the third/natural route. A narrow RED test and V5-only gate delayed third bases when an opponent army was within about `1450` of the two active mining bases and V5 had fewer than eight combat units. The exact match got worse, moving from a V3 win at `631.2s` to a V3 win at `606.4s`; this was reverted. Do not solve `silverRidge` by delaying the third itself. The useful signal is the later army conversion/fight quality around that third, not the existence of the third plan.
- Neutral/reverted forward-tower experiment on 2026-06-11: exact `silverRidge v5 south` showed V4-TR starting a low-HP tower near V5's home side while V5's field group still carried expansion claims. A V5-only attack-wave branch that redirected local expansion squads into unfinished enemy home-side towers flipped exact `silverRidge` to V5 wins, but the standard 50-map seed stayed flat at `75/100`: it gained `cloverRun v5 south`, `basaltMeadow v5 south`, `mapleCircuit v5 south`, and `silverRidge v5 south`, while losing `lichenCrown v5 south`, `graniteBloom v5 south`, `reedBasin v5 south`, and `bluebellHeath v5 north`. Tightening the branch against guarded towers fixed `lichenCrown` but lost `silverRidge` again. This was reverted. Do not add a generic forward-tower break rule without a better selector for tower race vs local army fight.
- Negative worker-pressure guard experiment on 2026-06-11: exact `ivoryTarn v5 north` showed V5 clicking a center worker while mixed V3/V4-TR guards stood on top of that target. A V5-only workerPressure branch redirected such raids into the guarded combat unit. The exact improved from a V3 kill at `640.9s` to a timeout with V5 `169` enemy kills, but the standard 50-map seed dropped from `75/100` to `73/100`; this was reverted. Do not broadly convert guarded worker-pressure targets into combat fights unless the lost raid tempo is replaced by a finishing/expansion payoff.
- Negative Grove first-ranged bank experiment on 2026-06-11: low-peak Grove losses showed first-natural clearing squads going in without stable ranged support, so a V5-only first-archer expansion-bank bypass was tested. The RED test verified the current planner idles through a mid first-expansion bank instead of training the first archer, but exact verification did not flip `silverRidge v5 south`, `lanternFord v5 north`, or `wispQuarry v5 north`; `wispQuarry` got much worse in exact replay, moving from the retained `793.85s` V4-TR win to a `476.7s` V4-TR win. This was reverted. Do not pierce the first-expansion bank just to add the first Grove archer; it delays the natural conversion more than it fixes the clear.
- Negative thin-natural minimum experiment on 2026-06-11: print diagnostics showed `wispQuarry v5 north`, `lanternFord v5 north`, and `silverRidge v5 south` first committing only three surviving melee bodies by `190s` after a four-body clear had already fed a unit to ranged neutral guards. A V5-only Grove 1v2 guard blocked four pure-melee bodies from clearing a first natural with at least `2.5` remaining neutral power, and the same check was wired through `expansion`, `objectiveControl`, and `attackWave`. The standard 50-map seed collapsed from `75/100` to `67/100`; exact `runeMeadow v5 south` degraded from timeout to a V4-TR win at `746.85s`. This was reverted. Do not solve thin first-natural attrition with a raw body-count gate; the correct fix must preserve natural timing, likely through micro/focus/healing conversion rather than waiting.
- Negative expansion-denial retreater experiment on 2026-06-11: exact `cloverRun v5 south` showed all six V5 Ember soldiers parked at a safe retreat rally with active retreat claims while no unoccupied expansion mines remained; `expansionDenial` also selected the highest-scored route-covered enemy mine before considering a safe north mine. A combined fix let route-covered denial candidates fall back, let safe stopped retreaters rejoin expansion denial, and made V5 worker pressure yield to a full-group denial. The exact improved from a V4-TR win at `1400.6s` to a timeout, but the standard 50-map seed dropped from `75/100` to `70/100` in run `2026-06-11T21-46-42-491Z-ovkyak`, with Grove falling `34/49 -> 29/49`; losses included `lichenCrown v5 south`, `graniteBloom v5 south`, `heatherCircuit v5 south`, `brackenFord v5 south`, and `mirrorHeath v5 north`. This was reverted. Do not broadly release safe retreat claims into cross-map expansion denial or globally prioritize expansion denial over worker pressure; the useful signal is no-free-mine closeout/denial, but it needs a selector that does not break Grove's local fight conversion.
- Negative first-armor bank experiment on 2026-06-11: exact `fernBarrow v5 north` confirmed V5 Grove already builds `stables`, `sanctum`, duplicate production, and `workshop` before dying; the missing value is not simply "more advanced buildings." A V5-only `weapon2/plating0` first-armor window passed RED tests by prioritizing `reinforcedPlating1` before weapon3 and holding routine training gold near the 165g armor cost. The exact replay got much worse: V4-TR win moved from `1352.1s` to `993.95s`, V5 enemy kills fell `64 -> 35`, moon-well healing fell `956 -> 391`, and item use fell `19 -> 5`. This was reverted. Do not bank mature Grove combat production for first armor by itself; the reduced body count loses field control before the armor pays back.
- Negative healthy worker-pressure detachment experiment on 2026-06-11: exact `viridianToll v5 south` showed V5 Grove losing its 680-750s fight while `workerPressure` and `skirmishPreservation` split the 10-body field group. A broader V5 Grove two-base rule that always reduced mature workerPressure to a 3-unit detachment passed the isolated RED test but made the exact worse, moving the V3 win from `1182.4s` to `988.2s` and reducing V5 kills `47 -> 38`. This was reverted. Do not solve formation breaks by shrinking every healthy two-base worker raid; only the wounded recovery split remains viable so far.
- Negative threatened-expansion skirmish-retreat experiment on 2026-06-11: exact `viridianToll v5 south` showed `skirmishPreservation` pulling units from a third-base fight back to main, so a V5 1v2 local-skirmish fallback to the threatened owned expansion was tested. It made `viridianToll` worse (`1182.4s` V3 win -> `1060.2s` V3 win) and broke the retained `fernBarrow v5 north` win (`1660.65s` V5 win -> `944.1s` V4-TR win). This was reverted. Do not hold every threatened expansion by changing the skirmish retreat point; the selector must distinguish saveable cover fights from bad expansion trades.
- Negative early weapon2 bank experiment on 2026-06-11: exact `viridianToll v5 south` showed V5 Grove had 7 weapon-benefiting units and 105g but kept training instead of banking for weapon2. A V5 Grove 1v2 rule lowering the two-base weapon2 bank threshold from 8 to 7 passed the isolated RED test, but made the exact much worse (`1182.4s` V3 win -> `905.55s` V3 win) and still never completed weapon2 before death. This was reverted. Do not open heavy-tech windows by reducing the weapon2 bank threshold alone; it removes the immediate bodies needed to survive the current fight.
- Neutral affordable-heavy experiment on 2026-06-11: a narrow V5 Grove rule was added so an already-affordable mature three-base workshop/stables window can prefer one `golem` or up to two `knight` bodies before another routine basic unit. The isolated RED test covers the intended non-banking behavior. The standard 50-map seed stayed flat at `75/100` in run `2026-06-11T23-32-38-124Z-ovkyak`, with no flips versus the previous `75/100` run `2026-06-11T22-31-37-532Z-ovkyak`. Keep under watch; this is not a proven score gain and should not be broadened into a bank.
- Negative expansion-approach recall experiment on 2026-06-11: exact `ochreRidge v5 south` suggested V5 Grove needed to recall old attack orders before a large enemy wave reached a mining expansion. A broad V5-only attack-wave expansion hold flipped the exact to a V5 win at `1140.4s`, but broke retained exact `heatherCircuit v5 south` (`1343.2s` V5 win -> `1239.75s` V4-TR win). A narrowed count/power version restored `heatherCircuit` but failed to retain the `ochreRidge` flip, and the standard 50-map seed collapsed from `75/100` to `67/100` in run `2026-06-11T23-53-40-412Z-ovkyak`, with Grove falling `34/49 -> 27/49`. This was reverted. Do not add generic non-main expansion recalls inside attackWave; they create oscillation between local fight, main hold, and expansion hold.
- Negative move-worker economy reclaim experiment on 2026-06-12: exact `runeMeadow v5 south` showed three surviving V5 workers parked on stale `move` orders while owned mines still had gold, so the economy script was tested with unclaimed moving workers treated as assignable miners. The exact flipped from timeout to a V5 win at `1038.8s`, but the standard 50-map seed collapsed from `75/100` to `62/100` in run `2026-06-12T00-09-13-664Z-ovkyak`; adding `workerDefense` and `constructionRecovery` move claims still scored only `63/100` in run `2026-06-12T00-13-58-505Z-ovkyak`. This was reverted. Do not let generic economy reclaim `move` workers; many move orders are tactical evacuation or construction positioning even when they are not explicitly claimed.
- Negative worker-move claim experiment on 2026-06-12: recording `constructionRecovery` moves as build claims and `workerDefense` evacuation moves as retreat claims looked architecturally cleaner, but without generic economy reclaim it still dropped the standard seed from `75/100` to `69/100` in run `2026-06-12T00-16-16-678Z-ovkyak`. This was reverted. `availableBuilder` and other claim-aware selectors currently depend on worker move claims being sparse; broadening claim ownership for workers requires a separate lifecycle redesign, not a local record-layer patch.
- Negative veteran-core early-pressure experiment on 2026-06-12: exact `wispQuarry v5 north` showed V5 had five healthy combat units including a level-3 archer plus one wounded footman at `540s`, but ordinary attackWave waited for the seven-body post-natural threshold. A V5 1v2 rule let a healthy veteran core pressure early while excluding wounded bodies and ignoring far-off second-opponent power; this flipped the exact from a V4-TR win at `793.85s` to a V5 win at `944.9s`. The standard 50-map seed still collapsed from `75/100` to `65/100` in run `2026-06-12T00-46-03-911Z-ovkyak`, with flips `+3/-13` against baseline run `2026-06-12T00-18-51-955Z-ovkyak`. This was reverted. Do not bypass the seven-body 1v2 wave threshold just because a veteran core exists; it creates too many premature fights and timeouts. Veteran/high-level value should be pursued through recovery, late composition, or fight-quality selectors, not smaller early attackWave timing.
- Negative settled-move-worker economy experiment on 2026-06-12: exact `runeMeadow v5 south` showed three unclaimed V5 workers stopped at a safe `move` point with live mines still available; a narrow V5-only economy rule reclaimed only settled safe move workers, leaving moving/threatened workers alone. This flipped the exact timeout to a V5 win at `869.6s`, but the standard 50-map seed dropped from `75/100` to `67/100` in run `2026-06-12T01-00-40-566Z-ovkyak`, with many timing flips across both races. This was reverted. Even settled worker move orders are entangled with tactical/economic timing; move-worker reclamation needs a lifecycle redesign or a higher-level recovery script, not inclusion in the ordinary economy assignable pool.
- Negative two-base knight window experiment on 2026-06-12: user-requested heavier late composition was tested by relaxing V5 Grove's already-affordable `knight` priority from three active mining bases to two, while keeping golems at three bases and keeping the rule non-banking. The standard 50-map seed dropped from `75/100` to `74/100` in run `2026-06-12T01-21-56-913Z-ovkyak`, with only one flip: retained win `obsidianBrook v5 north` became a V4-TR loss. This was reverted. Do not broaden heavy-unit priority only by lowering the active-base threshold; the current losses need production-chain/fight conversion evidence, not just earlier knights.
- Negative two-base caster-chain experiment on 2026-06-12: exact `viridianToll v5 south` showed V5 Grove building duplicate basic production before `sanctum`, then trying to add `sanctum` too late after the army had already collapsed. A broad V5 Grove two-base production gate that lowered both `stables` and `sanctum` timing flipped `viridianToll` to a V5 win, but dropped the standard 50-map seed from `75/100` to `72/100` in run `2026-06-12T01-39-37-056Z-ovkyak`. A narrower rule that only added `sanctum` after `barracks`/`archeryRange`/`stables` were complete still flipped exact `viridianToll` and restored exact `mallowRun`, but collapsed the same 50-map seed to `68/100` in run `2026-06-12T01-44-07-292Z-ovkyak`. This was reverted. Do not solve Grove heavy/caster variety by globally advancing `sanctum`; it disrupts the basic-production tempo that carries many retained wins.
- Negative late worker recovery experiment on 2026-06-12: exact `runeMeadow v5 south` timed out at `2400s` with V5 on two footmen, three stopped `move` workers at main, `69g`, and a live third-base mine. A very narrow V5 late-deadlock economy recovery was tested: after `1800s`, with gold below one footman, at most two combat units, no main pressure, and stopped unclaimed workers near main, those workers could be reassigned to a live owned mine. The exact still timed out, and the three recovered workers died, leaving V5 with only the two footmen. This was reverted without a 50-map run. Do not solve `runeMeadow` by late worker mining recovery; the timeout needs an earlier combat/closeout conversion, not sacrificing the last workers.
- V5 skirmish critical-pickoff micro slice on 2026-06-12: exact `viridianToll v5 south` showed `skirmishPreservation` running before `focusFire`, so a local disadvantaged skirmish could attack-move home while a high-XP ranged target was at `12 HP` and in range. V5 now lets the skirmish module finish an in-range non-neutral ranged target below `18%` HP when at least two damage-dealing allies can volley it down, before issuing the preservation retreat. Exact `viridianToll v5 south` still lost, but improved from a V3 win at `1182.4s` with V5 `47` enemy kills to a V3 win at `1471.55s` with V5 `95` enemy kills. The standard 50-map seed improved from `75/100` to `77/100` in run `2026-06-12T02-15-53-743Z-ovkyak`, with flips `ivoryTarn v5 north` and `pearlBog v5 south` from losses to V5 wins and no reverse flips. Keep this micro slice; it improves fight quality without adding a new strategic lane.
- Neutral/reverted production-chain experiment on 2026-06-12: exact `basaltMeadow v5 south` looked like Grove V5 had `sanctum`/`stables` before enough bodies, so a narrow V5 Grove rule was tested to prevent high gold from bypassing the two-base `sanctum` combat gate below eight combat units. The isolated production-model test passed, but exact `basaltMeadow v5 south` was unchanged (`951.95s` V3 win with the same V5 `30` kills and `83` units lost), and exact `marbleGrove v5 south` was also unchanged. This was reverted without a 50-map run. Do not add this high-gold sanctum gate; it does not hit the real timing that creates the current losses.
- Negative melee-pickoff expansion on 2026-06-12: broadening the V5 skirmish critical-pickoff rule from extreme ranged targets to all extreme non-neutral targets flipped exact `viridianToll v5 south` to a V5 win at `1494.75s`, but the standard 50-map seed collapsed from the retained `77/100` to `72/100` in run `2026-06-12T02-29-16-908Z-ovkyak`, with Grove falling `35/49 -> 32/49` and Ember `42/51 -> 40/51`. This was reverted. Do not chase critical melee/frontline last hits from the preservation module; the ranged-only version is the current proven boundary.
- Negative duplicate-production bank-break experiment on 2026-06-12: exact `cloverRun v5 south` showed V5 Ember at `960s` with two mining bases, idle `emberForge`/`cinderSpire`, `220g`, six combat units, and `duplicateCoreProductionReserveKind = cinderSpire`, while V3 had eighteen combat units. A V5-only rule was tested so base-range 1v2 pressure could spend duplicate-production reserve on current army. It improved the exact from a V4-TR win at `1400.6s` to an `1800s` timeout with V5 `56` kills, but the standard 50-map seed dropped from retained `77/100` to `70/100` in run `2026-06-12T02-52-36-607Z-ovkyak`. Flips were `+2/-9`, including losses of retained wins on `yarrowFen`, `heatherCircuit`, `reedBasin`, `ivoryTarn`, `pearlCircuit`, `mallowRun`, `lanternFord`, and `chalkFen`. This was reverted. Do not globally release duplicate-production banks under pressure; the damage is broad macro timing loss, not just local overproduction.
- Negative local stolen-third punish experiment on 2026-06-12: exact `plumTarn v5 south` showed V3 starting an unfinished town hall on V5's side third mine around `480s`, while all V5 combat units were locked in ordinary attack claims on a V3 footman and `expansionDenial` had no free squad. A V5-only `planExpansion` branch was tested to attack a local unfinished enemy side expansion before tactics. The exact improved from a V3 win at `941.15s` with V5 `61` kills to a V3 win at `1276.5s` with V5 `113` kills, but the standard 50-map seed collapsed from retained `77/100` to `65/100` in run `2026-06-12T03-14-10-936Z-ovkyak`, with flips `+3/-15`. This was reverted. Do not globally let side-expansion punish override ordinary attack claims; the local economic idea is valid but needs a selector that preserves current fight/pressure tempo.
- Neutral first-spark creep-tempo slice on 2026-06-12: user suggested one or two early ranged units for harassment/creeping. Direct checks showed the existing `earlyHarassment` script is not a travel opener; it only fires when harassers are already near workers, so simply adding it to V5 would be inert. A narrower Ember rule now lets V5 spend a guarded first-natural bank on its first `sparkArcher` when the natural still has guards, before the bank reaches town-hall cost. Focused tests pass, but the standard seed stayed flat at `77/100` in run `2026-06-12T03-59-46-453Z-ovkyak`, with no flips versus the retained `2026-06-12T02-56-22-111Z-ovkyak` run. Keep only under watch; this is not yet a measured score gain.
- Negative center-expansion punish removal on 2026-06-12: exact `cobaltVale v5 north` showed `planExpansion` attacking a V3 center town hall at `282s`, splitting four units away from two active creep claimants and causing an early collapse. A broad guard that stopped treating any next mine as a stolen natural once V5 had two active mining bases improved the exact from a `556s` V3 win to a later `785s` V3 win with V5 kills rising `9 -> 39`, but the standard seed collapsed from retained `77/100` to `65/100` in run `2026-06-12T04-23-02-117Z-ovkyak`. This was reverted. The center-expansion punish is mislabeled inside the stolen-natural path, but it is currently a high-value tempo weapon and cannot be globally removed.
- Negative four-unit center-punish minimum on 2026-06-12: a narrower TDD variant only blocked non-first-natural center punish attacks when active creep claims left fewer than five attackers. It flipped exact `cobaltVale v5 north` to a V5 win at `809s`, but still dropped the standard seed from retained `77/100` to `71/100` in run `2026-06-12T04-32-11-822Z-ovkyak`, with flips `+2/-8`. This was reverted. Do not weaken early center-hall punishment by simple squad-size thresholds; any future fix must preserve its winning-map tempo while solving specific split failures.
- Negative early-harassment stack experiment on 2026-06-12: directly inserting the existing opportunity-only `earlyHarassment` script into the V5 stack before `workerPressure` produced a correct local two-unit worker raid, but also required a post-conflict workerPressure integrity guard to avoid one-unit leftover raids. The standard seed collapsed from retained `77/100` to `68/100` in run `2026-06-12T04-58-46-167Z-ovkyak`, with Grove falling `35/49 -> 31/49` and Ember `42/51 -> 37/51`. This was reverted. Do not solve the user's "one or two early ranged harassers" idea by simply enabling the existing `earlyHarassment` lane; it disrupts early 1v2 grouping more than it adds worker pressure.
- Negative small dead-economy closeout experiment on 2026-06-12: exact `runeMeadow v5 south` showed a late timeout where both opponents had no workers and V5 had two healthy footmen idle, because `deadEconomyCloseoutReady` requires five soldiers. A narrow V5 1v2 two-to-four soldier closeout rule made the 1800s planner issue an `attackMove` toward the remaining V4-TR expansion, but exact replay still timed out and V5 enemy kills dropped from `160` to `140`. This was reverted without a 50-map run. Do not lower the dead-economy closeout size by itself; the exact needs earlier army preservation/cleanup, not a late two-footman march.
- Negative critical town-hall pickoff experiment on 2026-06-12: exact `runeMeadow v5 south` showed a V5 lancer chasing a worker beside a completed V4-TR expansion town hall at `5 HP`. A narrow V5 `attackWave` rule that used already-near `attack`-ordered units to finish such one-volley town halls passed the local RED test, but exact `runeMeadow v5 south` still timed out and the standard seed dropped from retained `77/100` to `75/100` in run `2026-06-12T05-29-26-903Z-ovkyak`, with Grove falling `35/49 -> 34/49` and Ember `42/51 -> 41/51`. This was reverted. Do not add one-hit building pickoff as an attack-wave side rule; it steals enough local tempo to hurt aggregate results without solving the timeout.
- Negative focused-worker-pressure experiment on 2026-06-12: `cloverRun v5 south` showed V5 initially focusing V4-TR, then switching focus memory to the live V3 threat after V3's army had already grown. A narrow V5 workerPressure rule made raids follow the remembered live focus owner instead of a nearer weak-side worker. The local RED test reproduced the target-selection issue, but exact `cloverRun v5 south` got much worse, moving from a V4-TR win at `1400.6s` with V5 `56` enemy kills to a V4-TR win at `864.5s` with V5 `31` enemy kills. This was reverted without a 50-map run. Do not force workerPressure to follow focus memory by itself; the current nearest-worker tempo is carrying survival even when the strategic focus looks stale.
- Negative second-healing reserve experiment on 2026-06-12: `cloverRun v5 south` showed V5 Ember with two bases, one `emberShrine`, wounded defenders, and main pressure, so a V5-only second-healing reserve/build rule was tested after the main guard tower existed. Focused tests passed, but exact `cloverRun v5 south` got worse, moving from the retained V4-TR win at `1400.6s` with V5 `56` enemy kills to a V4-TR win at `889.45s` with V5 `47` enemy kills. This was reverted without a 50-map run. Do not add a second healing-building bank by itself; the current losses need fight conversion/army formation, not more healing throughput after the static-defense bank.
- Negative unitless-focus-owner experiment on 2026-06-12: `ashVale v5 north` showed V5 keeping strategic focus on a unitless V4-TR residual-building owner while V3's live army scaled, so a V5-only focus selector skipped owners with no units when another opponent still had units. The isolated RED test passed and exact `ashVale v5 north` increased V5 kills, but the standard seed dropped from retained `77/100` to `76/100` in run `2026-06-12T06-52-36-017Z-ovkyak`; the only flip was retained win `juniperDelta v5 south` becoming a V3 loss. This was reverted. Do not discard residual-building focus globally; any future focus update needs a local threat/formation condition, not just unit presence.
- Negative critical-harasser exclusion experiment on 2026-06-12: `runeMeadow v5 south` showed late `workerPressure` reusing a `harass`-claimed critical-HP body, so a V5-only `harassmentReadyUnit` guard excluded harass claims below `36%` HP. The focused RED test passed, but exact `runeMeadow v5 south` collapsed from the retained timeout with V5 `160` enemy kills to a V4-TR win at `1096.1s` with V5 `69` enemy kills. This was reverted without a 50-map run. Do not solve the timeout by making critical harassers ineligible; the sacrificial worker-pressure lane is still buying essential delay/cleanup.
- Negative melee-mode focus-tail release experiment on 2026-06-12: `viridianToll v5 south` showed V5 Grove splitting when a small tail kept a remembered focus target, so the existing combat-mode stale-focus-tail release rule was applied to V5 1v2 melee mode. Exact `viridianToll v5 south` was roughly neutral, but the standard seed collapsed from retained `77/100` to `69/100` in run `2026-06-12T07-05-44-182Z-ovkyak` with flips `+3/-11` and Grove falling to `27/49`. This was reverted. Do not port combat-mode focus anchoring wholesale into melee; remembered focus stickiness is carrying many Grove wins even when it looks locally ugly.
- Negative V5 repair-stack experiment on 2026-06-12: V5 did not include the existing `repair` script while V4-TR did, so the V5 stack was tested with `repair` inserted after `emergencyDefense`. The focused preset test passed and exact `ashVale v5 north` increased V5 kills, but the standard seed dropped from retained `77/100` to `75/100` in run `2026-06-12T07-15-01-047Z-ovkyak`; both flips were retained Grove wins (`graniteBloom v5 south`, `umberCauseway v5 south`) becoming V4-TR losses. This was reverted. Do not attach generic tower repair to V5 without a stricter timing/worker-cost selector.
- Fresh V5 cross-seed pressure audit on 2026-06-12: after revalidating the prerequisite gates, seed `v5-goal-audit-50-2026-06-12a` scored only `68/100` in run `2026-06-12T07-30-12-534Z-1catfz9`, with V5 Grove `31/50`, V5 Ember `37/50`, V3 Grove opponent split `40/50`, V3 Ember opponent split `28/50`, and opponent-order splits `33/50` and `35/50`. This shows the retained `77/100` line is not stable across fresh 50-map samples; continue treating V5 as below gate and avoid tuning to only `v5-merc-bank-50-2026-06-10a`.
- Negative/neutral first-Ember-shrine expansion-bank probe on 2026-06-12: fresh seed `v5-goal-audit-50-2026-06-12a` scored only `68/100`, and exact `ashVale v5 north` showed V5 Ember dying at `391.3s` with zero healing while V3 Ember had an `emberShrine`. A RED test extended the existing V5 Grove cleared-natural first-healing-source rule to Ember, but exact replay stayed behavior-equivalent at the report level: V4-TR still won at `391.3s`, V5 still had `1` enemy kill, `22` units lost, `0` healing events, and no `healingWell` command. This was reverted without a 50-map run. Do not broaden the Grove first-natural healing rule to Ember as an isolated change; the real failure happens before that predicate becomes true.
- Negative two-base objective-ready HP threshold experiment on 2026-06-12: `hazelCircuit` and early Ember losses showed V5 reusing wounded first-natural survivors for optional neutral objectives, so a V5-only `objectiveReadyUnit` threshold excluded two-base units below `60%` HP from fresh `objectiveControl`. This flipped exact `hazelCircuit v5 north` and `hazelCircuit v5 south` to V5 wins, but the standard seed `v5-goal-audit-50-2026-06-12a` dropped from `68/100` to `66/100` in run `2026-06-12T07-55-55-998Z-1catfz9`, with flips `+5/-7`. This was reverted. Do not fix the wounded-objective symptom by raw HP thresholds; the selector must preserve objective tempo on maps like `mallowRun`, `pearlBog`, `fernBarrow`, `pearlCircuit`, `celadonPass`, `bluebellHeath`, and `yarrowFen`.
- V5 mercenary-claim ownership slice on 2026-06-12: exact `hazelCircuit v5 south` showed `mercenary` issuing a `hire` at the west bow post, which immediately cleared the camp claim; the later `objectiveControl` script in the same planner pass then stole the same freshly recovered squad into another objective. A RED claim test now treats `hire` as a shop transaction rather than movement completion, leaving camp movement ownership to expire through the existing objective-completion/abandon rules. Exact `hazelCircuit v5 south` flipped from a V3 win at `878.4s` to a V5 win at `726s`, while exact `mallowRun v5 north` remained a V5 win at `968.7s`. The fresh standard seed improved from `68/100` to `71/100` in run `2026-06-12T08-16-11-984Z-1catfz9`, with V5 Grove `31/50` and V5 Ember `40/50`.
- Negative runner-level hire reservation experiment on 2026-06-12: a narrower alternative reserved camp-controller units for only the same script-runner frame after `hire`, instead of preserving claim memory. It passed the isolated runner test but collapsed the fresh standard seed to `61/100` when both exact probes were accidentally run without `--details` and therefore executed the full 100-match benchmark. This was reverted. Do not block same-frame post-hire command tempo in the generic runner; some winning maps rely on immediate post-hire continuation. Keep any future fix in the claim/objective lifecycle where it can be conditioned by ownership state.
- Negative main-approach hold experiment on 2026-06-12: exact `amberReach v5 south` showed six fully recovered V5 Grove fighters idle at the main recovery point while five V4-TR mercenary/support units approached just outside the base hit window. A V5-only early `attackWave` hold point moved recovered retreaters toward the main approach before the tower foundation became the pressured building. The exact improved locally from a fast V4-TR win with V5 `6` enemy kills to a later V4-TR win at `657.4s` with V5 `22` enemy kills and `284` moon-well healing HP, but the fresh standard seed dropped from retained `71/100` to `69/100` in run `2026-06-12T10-47-11-535Z-1catfz9`. This was reverted. Do not solve the six-body recovered-defender symptom by adding a generic pre-contact approach hold; it improves one local defense while damaging aggregate 1v2 timing.
- Negative duplicate-production reserve break experiment on 2026-06-12: the same `amberReach v5 south` window also exposed a macro no-action gap where V5 had `185g`, idle core production, six defenders, and a duplicate-`stables` reserve that would not build until `260g`. A V5-only rule allowed one combat unit when two-base V5 was below its seven-unit attack-wave threshold and a stronger combined 1v2 army was entering main-approach range. Exact `amberReach v5 south` improved locally to a later V4-TR win at `628.85s`, with V5 `25` enemy kills, `554` moon-well healing HP, and peak supply `43`, but the fresh standard seed still dropped from retained `71/100` to `70/100` in run `2026-06-12T11-00-00-137Z-1catfz9`. This was reverted. Do not break duplicate-production reserve solely for the seventh defender; the aggregate macro timing loss still outweighs the local defense improvement.
- V5 late-upgrade system slice on 2026-06-12: added three ordinary-combat-unit upgrades through the existing research chain instead of a special AI-only rule. `speedTraining` gives total movement speed multipliers of `1.25/1.38/1.50` and does not affect attack speed. `rangeTraining` gives total unit attack-range multipliers of `1.15/1.25/1.35` and does not affect buildings or mercenaries. `leadership` gives starred ordinary combat units `+1/+2/+3 HP/s per star` through sim tick regeneration; it does not emit per-tick heal effects and does not use moon-well cooldown logic. Grove researches them at `stables`/`workshop`/`sanctum`; Ember researches them at `cinderSpire`. UI coverage now includes research buttons, progress buttons, labels, tooltips, and zh/en text via the same research catalog. V5 is the only AI version that actively chooses these new late upgrades; earlier versions can still obey player/manual research commands but do not plan them. Focused verification: `npx vitest run src/shared/sim.test.ts src/shared/command-schema.test.ts src/shared/sim/command-validation.test.ts src/client/research-controls.test.ts src/client/tooltips.test.ts src/client/hud-model.test.ts src/client/command-button-state.test.ts src/client/selection-controls.test.ts src/ai/policy.test.ts` passed with `501/501`, and `npm run build` passed. On pgl 50-map samples, retained seed `v5-goal-audit-50-2026-06-12a` improved from `71/100` to `80/100` in run `2026-06-12T11-23-58-650Z-1catfz9`, with V5 Grove `40/50` and V5 Ember `40/50`; fresh seed `v5-late-upgrades-50-2026-06-12a` scored `83/100` in run `2026-06-12T11-25-16-288Z-e6iffm`, with V5 Grove `40/51` and V5 Ember `43/49`. A follow-up no-empty-leadership trigger, requiring veteran stars instead of excess gold before V5 buys `leadership`, preserved both `80/100` and `83/100` on runs `2026-06-12T11-32-18-280Z-1catfz9` and `2026-06-12T11-32-56-495Z-e6iffm`; keep it because it matches the upgrade purpose without score loss.
- Negative mercenary leadership-regeneration experiment on 2026-06-12: a broader interpretation that let `leadership` heal mercenary/contract combat units was tested because V5 often hires high-XP contract archers in late games. On the retained seed, the score dropped from `80/100` to `79/100` in run `2026-06-12T11-38-33-281Z-1catfz9`. This was reverted. Keep `leadership` tied to catalog ordinary-combat affected units; do not special-case hired units into the bottom-level regeneration path without fresh evidence.
- Negative two-base veteran worker-pressure detachment experiment on 2026-06-12: exact `sableRun v5 north` showed V5 Grove sending three level-1 ordinary fighters plus non-star units into `workerPressure` around `400s`, then losing the star core by `440s`. A narrow rule was tested so two-base V5 with at least three ordinary-combat star value would keep starred ordinary units out of `workerPressure` when three or more non-star raiders were available. Exact `sableRun v5 north` lasted longer (`891.1s -> 1001s`) but V5 kills fell (`44 -> 33`), and the fresh standard seed dropped from `83/100` to `82/100` in run `2026-06-12T11-55-06-146Z-e6iffm`, with Grove falling to `38/51`. This was reverted. Do not replace whole-army worker pressure with a simple non-star detachment rule; veteran preservation needs a fight-context selector that preserves Grove's midgame kill pressure.
- V5 shared early-tech stack slice on 2026-06-12: V5 uses V2 behavior semantics but its custom stack had skipped the existing `earlyTech` script before `productionBuilding`, so priority weapon timing could be delayed until the later generic `tech` pass. V5 now reuses the shared `earlyTech` primitive immediately after `economicCatchUp` and before `productionBuilding`; no new upgrade logic was added. The focused RED covers a five-combat, 160g Grove window where V5 researches `weaponTraining` before the next production building. Exact `heatherCircuit v5 north` was unchanged because that loss never reached the 140g post-five-combat bank. Fresh seed `v5-late-upgrades-50-2026-06-12a` improved from `83/100` to `84/100` in run `2026-06-12T12-06-18-767Z-e6iffm`, flipping only `rainbarrow v5 north` from timeout to V5 win with `weapon2` and `speed1`. Retained seed `v5-goal-audit-50-2026-06-12a` stayed `80/100` in run `2026-06-12T12-07-16-879Z-1catfz9` with no winner flips. Keep this small stack-unification gain.
- Negative V5 two-base catch-up third experiments on 2026-06-12: exact `wispQuarry v5 north` at `500s` showed V5 Grove with two mining bases, `400g`, five combat units, and a safe unoccupied third mine, but `planExpansion` returned empty because `missingCombatProductionKind = stables` while `productionBuildingNeedKind` was still below its duplicate-stables threshold. A narrow V5 Grove rule allowed a five-fighter catch-up third to precede duplicate `stables`, but the fresh seed `v5-late-upgrades-50-2026-06-12a` dropped from `84/100` to `82/100` in run `2026-06-12T12-37-13-912Z-e6iffm`, losing retained wins on `celadonPass v5 south` and `hazelCircuit v5 south` with no gains. A second variant also released the two-base third bank into training when `mainBaseNeedsObjectivePause` blocked the expansion; it flipped `wispQuarry v5 north` but dropped the same seed further to `81/100` in run `2026-06-12T12-33-33-185Z-e6iffm` with net `+1/-4` flips. Both were reverted. Do not break the two-base third/stables/army bank by simple five-body or main-pressure gates; the aggregate evidence says the current bank is preserving more winning timings than it strands.
- V5 baseline reset after prerequisite repair on 2026-06-12: the V5 mercenary-claim ownership rule was narrowed so ordinary V2/V3/V4-TR `hire` clears camp claims while V5 keeps the same-frame guarded-camp preservation through an explicit runner option. This fixed a real V3 Grove regression, so the hybrid opponent is stronger again. Fresh pgl seed `v5-late-upgrades-50-2026-06-12a` now scores `80/100` in run `2026-06-12T13-03-49-694Z-e6iffm`, with V5 Grove `40/51`, V5 Ember `40/49`, against V3 Grove `40/50`, against V3 Ember `40/50`, and opponent-order split `42/50` for `v3,v4-tr` versus `38/50` for `v4-tr,v3`. Treat this as the current honest V5 baseline; do not weaken V3 to recover the earlier `84/100`.

### 2026-09-24 Recovery After V4-TR Route-Clearing Towers

`f459512` (V4-TR route-clearing towers plus tower building targeting) dropped retained seed `v5-hybrid-50-2026-06-12` from `82/100` to `58/100`. Bisecting showed the sim targeting change alone kept `82/100`; the loss came from V4-TR no longer banking gold behind its route-safety rule, so both opponents now reach V5 together around `220s`.

Evaluation used five seeds (`v5-hybrid-50-2026-06-12`, `v5-hybrid-50-holdout-a` through `-d`, 500 games, 10 local workers):

- `main` at `d5a5b2d`: `315/500` (`58, 68, 66, 66, 57`).
- Guarded-expansion incoming-army gate: `338/500`. V5 in 1v2 does not send its army to clear a guarded mine while enemy combat power within `2200` of its main exceeds `0.8x` its own; this was the recurring `~190s` creep, `~220s` double-hit collapse.
- Plus attack-wave commitment holds: `345/500` (`74, 67, 64, 72, 68`). `locallyBeatableOpponentBaseTarget` and `closeoutAttackWaveTarget` sat on thresholds, so the army flipped between a base strike and the generic wave point every think and never arrived. A detector over 16 games counted ~200 such flips before and 14 after.

Rejected on the same five seeds (each flat or worse, reverted): wider main emergency-tower trigger, releasing the cleared-expansion bank under incoming pressure, gating objectiveControl and expansionDenial on the same incoming check, removing expansionDenial, adding `desperateWorkerFight` to the V5 stack, one-base worker labor `+1` instead of `+3`, and a V5 late-standoff counter push that counts towers.

Remaining losses: about six early double-rush collapses per 100 games, midgame trades against the combined army, and timeouts where V4-TR banks thousands of gold behind forward towers.

### 2026-09-25 Tower Breaker

Loss analysis of the `345/500` build showed V4-TR winning by creeping 200 HP towers into V5's base: in lost games V4-TR raised 5.1 towers within 700 of V5 buildings (2.3 before 600s) versus 1.2 in won games, and 4.4 of V5's 11.8 lost buildings died inside enemy tower range. Unit auto-targeting prefers units over towers, so V5 fought around the towers while they kept firing.

The new V5-only `towerBreaker` tactic (after `focusFire` in the V5 stack):

- kills the worker standing at an unfinished enemy tower site within 900 of any V5 building (construction advances for any owner worker at the site);
- otherwise sends every healthy nearby fighter (within 1600, at least three) at the nearest threatening enemy tower (unfinished first), when their power beats local defenders plus covering towers. A tower threatens when it is within 900 of a V5 building, of the mine V5 wants next, or within 700 of the V5 army.

Ten seeds (`v5-hybrid-50-2026-06-12`, `v5-hybrid-50-holdout-a` through `-i`, 1000 games):

- previous build `244d538`: `679/1000` (`74 67 64 72 68 61 70 66 67 70`);
- tower breaker near V5 buildings only: `739/1000`, every seed up;
- plus the wanted-mine and army-reach scope: `777/1000`;
- plus the builder snipe: `791/1000` (`80 84 82 85 74 75 76 74 78 83`); the retained seed is `80/100`.

Noise calibration: unrelated changes move a 1000-game total by about 15-20, so five-seed screens were not trusted. Rejected on ten seeds (flat or worse): removing the routine `defense` towers, earlier weapon or plating upgrades, a home-ground hold for wounded units, HP-aware skirmish retreat odds (`707`), camp-worker sniping, taking a guarded mercenary camp before the natural (`756`-`775`), holding a camp worker, and an opening creep hold (`688`).

Remaining losses on the retained seed: ten early collapses around 280-340s where V4-TR's mercenary army (and V3) reach V5 at about 220s while V5 has three or four units, eight midgame losses, and two timeouts. Separately, V4-TR loses workers to neutral camps in the first 90 seconds in 69 of 100 games; V5 wins 78% of those and 47% against a V4-TR that opens cleanly, so a later V4-TR opening fix will lower this score.

### 2026-09-25 Army First When Outnumbered

Found by replaying the early-collapse games by hand (reedBasin, graniteBloom, heatherCircuit on the retained seed) and by reading the opponents' attack rules: V3 treats V5 as its only opponent (1v1 rules) and V4-TR attacks as soon as it has five hired fighters. In every early collapse both opponents opened army-heavy (about six fighters between them at 150s against V5's three) while V5 kept building workers, a well, tech and creeping its natural into the first wave.

Before 300s, when both opponents' fighters together are at least twice V5's army (five or more of them), V5 now stops adding workers, skips the healing well, upgrades, early tech, economic catch-up and routine towers, and does not start creep fights. In all three replayed games V5 then held the first wave at home and was alive and expanding at 420-480s.

- ten seeds: `791/1000` -> `821/1000` (`90 82 81 85 75 83 81 82 80 82`); the retained seed is `90/100`;
- untouched final seeds (`v5-final-1` through `-5`): `398/500` -> `410/500`.

Keeping the mode on after 300s was worse (`805/1000` capped at 600s, `803/1000` uncapped): it is an opening answer, not a posture.

### 2026-09-25 What Decides The Remaining Losses (No Retained Change)

Analysis of the 179 ten-seed losses of `282cd0b` (timelines, decisive-fight windows, replays and save/load play):

- V4-TR's opening decides most of the score. When V4-TR has no workers left at 300s (it walks them into neutral camps; 387 of 1000 games) V5 wins 98%. Against a V4-TR still on six workers (552 games) V5 wins 71%, and those games hold 158 of the 179 losses. Within them, V5's own state at 300s is identical in wins and losses (army power 12.7, six fighters, nine workers); the enemies are slightly stronger in the losses and the game turns in the 300-420s fights.
- One town hall at 300s: 222 games at 65% (two halls: 87%). What holds the natural back is the guarded-expansion incoming-army gate (enemies within 2200 of the main outweighing 0.8 of V5's army), often V4-TR's mercenaries idling at their own camp.
- 121 of the 179 decisive losses happen within 900 of V5's main. There the home defenders match the attackers (median power 14 vs 14), but the enemy is 2.4 times V5 across the map and V5 trades 0.39 of what it loses.
- Worker feeding: 36 losses lose at least 15 workers before the final minute by rebuilding miners at a base under enemy towers or a parked army (auricDelta south: 93 workers built and killed between 600s and 1260s).
- Script churn: in a lost fight one army receives five contradictory macro orders in 13s (focus fire, raid recall, a creep camp, home, then a tower break into V3's army).

Tried against these and rejected on the ten tune seeds (baseline `821`):

- clearing the guarded contract-archer camp beside the main during the outnumbered opening: `798` (hollowFord `6/12` -> `12/12`, ironMoss `10/16` -> `16/16`, but russetBrook `14/14` -> `6/14`, saffronFen `20/20` -> `12/20`); the baseline hires the same three archers anyway, about 100s later, after the natural;
- keeping wounded units swinging within 750 of a town hall: stopped at `149/200` vs `172`;
- natural clearing without the outnumbered gate: `805`;
- camp clear plus the arrival gate: `804`;
- not mining or rebuilding workers at a contested base (enemy tower or stronger local army): stopped at `318/400` vs `338`; narrowed to bases that lost three workers in the last minute: `815`;
- two fast units hunting V4-TR's lone camp-hiring worker anywhere on the map: `818`;
- a four-second lease that stops macro scripts re-tasking each other's units: `793`;
- tower breaker counting every enemy fighter within 1000 of the tower: `792`.

Noise calibration: each of these flips 90-250 of the 1000 games in both directions. The simulation is deterministic, so every game a change touches becomes a new trajectory; a change has to be worth well over 20 games to be seen on ten seeds. mac1 (node 26) reproduces the A100 (node 22) baseline exactly (`821`, same per-seed counts). Ten fresh seeds (`v5-extra-1` through `-10`) give `792/1000` at `282cd0b`.

### 2026-09-25 Natural Clear Against Arriving Armies

The guarded-natural clear no longer waits on every enemy within 2200 of the main. The clear is a short job: walk out, kill the guards, walk home. V5 now counts only enemies whose own speed brings them to the natural inside that time (slowest squad member for the walks, squad damage against guard HP for the kill), and still refuses when those outweigh 0.8 of its army. A mercenary ball idling at V4-TR's camp across the map no longer freezes V5 on one base.

Small but positive on all three independent seed sets:

- ten tune seeds: `821/1000` -> `825/1000` (`91 84 81 82 76 87 81 84 79 80`);
- ten fresh seeds (`v5-extra-1` through `-10`): `792/1000` -> `799/1000`;
- untouched final seeds: `410/500` -> `420/500` (`85 85 86 80 84`).

Together `+21` over 2500 games, inside the per-set noise but never negative.

Still open after it: of 39 games that sat on one hall at 300s, 34 still do. Between 150s and 300s their expansion is held by real danger rather than by far-away armies: an enemy army within 1550 of the main (92 checks), fewer than four idle fighters (64), the outnumbered opening (62) and the unfinished production chain (52). These games are decided by the first home defense against a healthy V4-TR plus V3.

Rejected the same night: judging armies by damage per second instead of damage per hit for V5 only (a mercenary lands 31 per second, twice a footman, and was scored as 1.35 footmen): fresh seeds `799` -> `773`, tune seeds flat. Seeing the enemy as stronger made V5 more careful, and the caution cost more than the fights it avoided.

Replayed by hand (thornedDelta south, `v5-hybrid-50-holdout-e`): V5 clears its natural at 190-210s with three or four units, loses one and leaves three badly wounded with no healing building, then trains nothing from 220s to 260s while it banks the town hall, and meets V4-TR's eight-unit mercenary wave at 310s with five wounded units and 50 gold. Spending that bank on a shrine and fighters instead gave nine units at 300s and turned the first V4-TR approach away, but the game was still lost by 417s: the nine Ember units split across the base and V4-TR's mercenaries (about 166 damage per second against their 114) took them apart. As a rule ("no town-hall bank during the outnumbered opening") it was flat: fresh seeds `799` -> `794`, first seven tune seeds `582` -> `577`.

### 2026-09-25 V5 Fight Arena

The 1v2 benchmark could not rate a fighting change: any behaviour change sends 90-250 of the 1000 games down a new path, so a real gain in fights disappears in the reshuffle. `scripts/ai-v5-arena.ts` captures the moment ten seconds before V5's worst 30 seconds of each tune game (every fighter with its health, experience and carried items, every complete building, both sides' upgrades, no workers, no gold) and replays it on the same map with the real V5, V3 and V4-TR policies for 90 seconds (`src/ai/benchmark/v5-arena.ts`; scenario gold, upgrades and `hpRatio` seeds in the simulation). The score is enemy unit value killed minus V5 unit value lost; the V5 building value lost is reported next to it.

Ten tune seeds give 654 unique fights (identical games repeat across seeds). Each is run in six one- or two-pixel variants, 3924 replays in about 45 seconds on the A100. At `3cb7fed`:

- all fights: V5 trades `1.54` (enemy value killed per value lost), net `+264` per fight;
- fights from lost games (118): `1.13`, and V5 loses 569 building value per fight;
- home defense (101 fights, V5 army at its hall with enemies inside 1200): net `+282`, `416` building value lost per fight; open field (324): `+291`; V5 attacking an enemy hall (229): `+218`, trade `1.44`.

A paired per-fight comparison has a standard error of about 5 per fight on home defense and 13-15 on the field and attack sets, so a change must be worth roughly 25 value per fight to be seen.

First module results on it: focusing the enemy with the most damage per second per remaining health was neutral to slightly worse; holding march leaders until the group caught up was `+4` per fight (standard error 8), neutral. The heaviest home losses are fights V5 was never going to win (six fighters against fifteen, eight against twenty-three), so the home-defense gap is army size when the wave lands more than fighting.

Rejected before the arena, on the full benchmark: a V5 commander that owns the army while a wave is on or closing on a town hall. Engaging the wave itself lost `147` wins over eight tune seeds (it chased enemies 1300 away and fed new units in one at a time); holding the point and fighting only attackers kept 3 of 12 sample wins; only recalling far squads and pausing errands scored `770` tune and `740` fresh (pausing alone `780`, recalling alone `792`). The existing base-defense logic plus V3's own stopline (V3 turns back from a strong army at home) beat every version of it.

### 2026-09-25 Ranged Core

The arena answered a question the 1v2 benchmark never could: what V5's army should be made of. Replaying the same 654 fights with V5's own units swapped:

- its archers swapped for melee: trade `1.54` -> `1.11`, about `-245` value per fight on every kind of fight;
- its melee swapped for archers (Grove) or spark archers (Ember), at the same or slightly higher cost: trade `2.61`, net value per fight nearly doubled, fights won `2754` -> `3151` of `3924`, `+422` per fight in home defense;
- keeping two melee bodies in front of the archers: worse than none (`2.15`), because the melee runs ahead and pulls the shooters in.

V3 and V4-TR field footmen, ravagers and mercenaries that run at a kiting line and die on the way in. V5 now builds its shooter building first (archery range for Grove, cinder spire for Ember), trains from barracks or forges only until that building stands, adds more ranges or spires (up to three) where it used to duplicate core production, and keeps its spire on spark archers with one acolyte once six fighters stand.

Two more arena-priced modules ride on it:

- stutter step: a shooter whose weapon is reloading steps away from a melee attacker by exactly the distance it can walk before the next shot (`2.61` -> `2.75` trade on shooter armies; `+64` per fight on home defense, `+46` attacking);
- upgrades priced on shooter armies, per extra level: range `+93` per fight, speed `+90`, weapons `+55`, plating `+42`. Range and speed training now come first once four shooters stand (three more per level).

Results (1000-game sets, fresh and final seeds untouched while building):

- ten tune seeds: `825` -> `918` (ranged core) -> `935` (with stutter step and shooter upgrades), per seed `93 95 95 94 91 91 93 96 91 96`;
- ten fresh seeds (`v5-extra-1` through `-10`): `799` -> `915` -> `930` (with stutter step) -> `941`;
- untouched final seeds: `420/500` -> `477/500` (`92 99 95 95 96`).

Together `2353/2500` (94.1%), from `2044/2500` (81.8%).

## Non-Goals

- Do not close or regress the V3 and V4-TR gates while building V5.
- Do not add map-specific exemptions.
- Do not introduce a second gameplay implementation.
- Do not count local full benchmark evidence as acceptance evidence.
